<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:clipToPadding="false">

    <android.support.v7.widget.CardView
        android:id="@+id/grocr_card_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:cardCornerRadius="4dp"
        app:layout_constraintBottom_toTopOf="@id/grocr_item_label"
        app:layout_constraintTop_toTopOf="parent"
        app:cardElevation="6dp"
        app:cardMaxElevation="10dp">

        <ImageView
            android:id="@+id/grocr_item_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/avocado"
            app:layout_constraintBottom_toTopOf="@id/grocr_item_label" />

    </android.support.v7.widget.CardView>

    <TextView
        android:id="@+id/grocr_item_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/karla_bold"
        android:textColor="@color/grocer_item_label_color"
        android:textSize="17sp"
        android:textStyle="bold"
        android:paddingTop="7dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tool:layout_editor_absoluteX="16dp"
        tool:text="Avocado" />

</android.support.constraint.ConstraintLayout>